%h1 Your goal is to "#{current_user.goals}"

%h3 You must eat #{ @diet_calories } kcal, because your daily activity is "#{ current_user.activity }"

-if current_user.activity == "High" || current_user.activity == "Medium"
	%h2 In "#{current_user.activity}" activity we recoment to eat in diet 60% carbs, 20% protein and 20%fat
	%h2 In your case, Carbs = #{ @diet_calories * 0.6} kcal, Protein = #{ @diet_calories * 0.2} kcal, Fat = #{ @diet_calories * 0.2} kcal
-elsif current_user.activity == "Low" || current_user.activity == "Sedentary"
	%h2 In "#{current_user.activity}" activity we recoment to eat in diet 50% carbs, 25% protein and 25%fat
	%h2 In your case, Carbs = #{ @diet_calories * 0.5} kcal, Protein = #{ @diet_calories * 0.25} kcal, Fat = #{ @diet_calories * 0.25} kcal
